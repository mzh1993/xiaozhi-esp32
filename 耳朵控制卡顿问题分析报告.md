# 耳朵控制卡顿问题分析报告

## 一、问题现象

### 1. 耳朵控制不丝滑，出现"咔咔咔"短间隔抽搐

**日志表现**：
```
I (15973) TC118S_EAR_CONTROLLER: Moving both ears: combo=0, duration=55 ms
I (16033) TC118S_EAR_CONTROLLER: MoveBoth action change: 0 -> 1
I (16033) TC118S_EAR_CONTROLLER: Moving both ears: combo=1, duration=55 ms
```

**问题特征**：
- 动作在 `combo=0` 和 `combo=1` 之间快速切换（约60ms间隔）
- 电机快速反转，产生机械噪音（"咔咔咔"声）
- 动作看起来不流畅，有抽搐感

### 2. 串口连接中断

**日志表现**：
```
--- Error: device reports readiness to read but returned no data (device disconnected or multiple access on port?)
--- Waiting for the device to reconnect....
```

**问题特征**：
- 在耳朵动作执行期间出现串口连接中断
- 通常在快速动作切换后发生
- 需要等待设备重连

## 二、根本原因分析

### 2.1 Worker Task阻塞违反零阻塞原则

**问题代码位置**：`main/ear/tc118s_ear_controller.cc:1134-1156`

```cpp
void Tc118sEarController::StartBothWithStagger(ear_combo_action_t combo_action, uint32_t duration_ms) {
    switch (combo_action) {
        case EAR_COMBO_BOTH_FORWARD:
            SoftStartSingleEar(true, EAR_ACTION_FORWARD);
            if (EAR_START_STAGGER_MS > 0) {
                vTaskDelay(MS_TO_TICKS_MIN1(EAR_START_STAGGER_MS));  // ❌ 阻塞60ms
            }
            SoftStartSingleEar(false, EAR_ACTION_FORWARD);
            break;
        // ...
    }
}
```

**问题分析**：
1. `StartBothWithStagger()` 在 **Worker Task 上下文中执行**（通过 `PeripheralWorkerTask()` 调用）
2. 使用 `vTaskDelay(60ms)` **阻塞了 Worker Task**，违反了"定时器回调零阻塞"原则
3. 如果在此期间收到新的动作请求，会被延迟处理，导致动作重叠

### 2.2 动作快速切换时的状态竞态

**问题代码位置**：`main/ear/tc118s_ear_controller.cc:434-530`

```cpp
esp_err_t Tc118sEarController::MoveBoth(ear_combo_param_t combo) {
    // ...
    // 检查是否为相同动作的再入
    bool same_action = is_moving && (previous_action == combo.combo_action);
    
    // 如果是动作变化，会立即刷新GPIO
    switch (combo.combo_action) {
        case EAR_COMBO_BOTH_FORWARD:
        case EAR_COMBO_BOTH_BACKWARD:
            StartBothWithStagger(combo.combo_action, combo.duration_ms);  // ❌ 阻塞60ms
            break;
    }
    
    // 更新状态
    UpdateComboState(true, combo.combo_action, action_start_time_ms);
    
    // 重新调度停止定时器
    ScheduleComboStop(duration_ms);  // ❌ 会刷新之前的定时器
}
```

**问题分析**：
1. 当动作快速切换时（如序列中的 `FORWARD -> BACKWARD -> FORWARD`），每次 `MoveBoth()` 都会调用 `StartBothWithStagger()`
2. `StartBothWithStagger()` 的错峰延迟（60ms）会**累积阻塞 Worker Task**
3. 如果动作切换间隔小于错峰时间（60ms），会导致：
   - 上一个动作的错峰延迟还在执行
   - 下一个动作已经到达并开始执行
   - 电机在短时间内快速反转，产生"咔咔咔"声

### 2.3 序列定时器与停止定时器冲突

**问题代码位置**：`main/ear/tc118s_ear_controller.cc:735-795`

```cpp
void Tc118sEarController::OnSequenceTimer(TimerHandle_t timer) {
    // 投递动作到Worker队列
    auto task = std::make_unique<Application::PeripheralTask>();
    task->action = Application::PeripheralAction::kEarSequence;
    task->combo_action = static_cast<int>(combo.combo_action);
    task->duration_ms = combo.duration_ms;
    app.EnqueuePeripheralTask(std::move(task));
    
    // 移动到下一步
    current_step_index_++;
    
    // 设置下一步的定时器
    if (sequence_active_) {
        uint32_t total_time = step.duration_ms + next_delay;
        xTimerChangePeriod(sequence_timer_, MS_TO_TICKS_MIN1(total_time), 0);
    }
}
```

**问题分析**：
1. 序列定时器根据 `duration_ms + delay_ms` 设置下一步触发时间
2. 但 `ScheduleComboStop()` 会在每次 `MoveBoth()` 时**重新刷新停止定时器**
3. 如果动作快速切换，停止定时器会被不断刷新，可能导致：
   - 动作无法按时停止
   - 序列定时器与停止定时器不同步
   - 动作重叠或提前终止

### 2.4 Worker Task优先级与阻塞冲突

**问题代码位置**：`main/application.cc:1020-1027`

```cpp
void Application::PeripheralWorkerTask() {
    // ...
    // speaking 首包窗口期（2s）抑制大动作
    if (device_state_ == kDeviceStateSpeaking &&
        last_tts_start_time_ms_ > 0 &&
        (now_ms - last_tts_start_time_ms_) < 2000) {
        uint64_t remain = 2000 - (now_ms - last_tts_start_time_ms_);
        vTaskDelay(pdMS_TO_TICKS((uint32_t)remain));  // ❌ 可能阻塞2秒
    }
    
    // 执行动作
    ear->MoveBoth(combo);  // ❌ 内部还会阻塞60ms
}
```

**问题分析**：
1. Worker Task 优先级为5，低于音频任务（4）但高于其他低优先级任务
2. 在 speaking 首包窗口期，Worker Task 可能阻塞**最多2秒**
3. 加上 `StartBothWithStagger()` 的60ms阻塞，总阻塞时间可达**2060ms**
4. 长时间阻塞可能导致：
   - 串口通信任务被延迟（如果优先级较低）
   - 其他外设动作无法及时响应
   - 系统响应延迟

## 三、优化方案

### 3.1 移除 StartBothWithStagger 中的阻塞

**目标**：将错峰逻辑改为非阻塞实现，避免阻塞 Worker Task

**方案A：使用定时器实现错峰**（推荐）
- 左耳立即启动
- 使用 `esp_timer` 延迟60ms后启动右耳
- 避免阻塞 Worker Task

**方案B：移除错峰逻辑**（简化）
- 如果错峰不是必须的，可以直接移除
- 双耳同时启动，减少复杂度

### 3.2 优化 MoveBoth 的再入逻辑

**目标**：当动作变化时，应该立即刷新GPIO，而不是等待错峰延迟

**改进方案**：
```cpp
esp_err_t Tc118sEarController::MoveBoth(ear_combo_param_t combo) {
    // ...
    bool same_action = is_moving && (previous_action == combo.combo_action);
    
    // 如果动作变化，立即停止当前动作并刷新GPIO
    if (is_moving && previous_action != combo.combo_action) {
        // 立即停止当前动作
        StopBoth();
        // 等待一小段时间确保电机停止（非阻塞，使用定时器）
        // 或者直接刷新新动作（如果硬件支持）
    }
    
    // 执行新动作（非阻塞）
    // ...
}
```

### 3.3 优化序列定时器与停止定时器的同步

**目标**：确保序列定时器与停止定时器不会冲突

**改进方案**：
1. **停止定时器只负责停止，不控制序列推进**
2. **序列定时器根据动作实际完成时间设置下一步触发时间**
3. **如果动作被提前停止，序列定时器应该相应调整**

### 3.4 移除 Worker Task 中的长时间阻塞

**目标**：speaking 首包窗口期的延迟应该使用定时器，而不是 `vTaskDelay`

**改进方案**：
```cpp
void Application::PeripheralWorkerTask() {
    // ...
    // speaking 首包窗口期：投递延迟任务而不是阻塞
    if (device_state_ == kDeviceStateSpeaking &&
        last_tts_start_time_ms_ > 0 &&
        (now_ms - last_tts_start_time_ms_) < 2000) {
        uint64_t remain = 2000 - (now_ms - last_tts_start_time_ms_);
        // 使用定时器延迟执行，而不是阻塞
        ScheduleDelayedPeripheralTask(std::move(task_ptr), remain);
        continue;
    }
    
    // 立即执行动作（非阻塞）
    // ...
}
```

## 四、实施优先级

### 高优先级（立即修复）

1. ✅ **移除 `StartBothWithStagger()` 中的 `vTaskDelay()`**
   - 影响：消除60ms阻塞，减少动作重叠
   - 风险：低
   - 工作量：1-2小时

2. ✅ **优化 `MoveBoth()` 的动作切换逻辑**
   - 影响：动作切换更流畅，减少"咔咔"声
   - 风险：低
   - 工作量：2-3小时

### 中优先级（短期优化）

3. ⚠️ **移除 Worker Task 中的长时间阻塞**
   - 影响：避免串口连接中断
   - 风险：中（需要测试speaking期间的行为）
   - 工作量：3-4小时

4. ⚠️ **优化序列定时器与停止定时器的同步**
   - 影响：序列动作更精确
   - 风险：中（需要测试各种序列）
   - 工作量：4-5小时

### 低优先级（长期优化）

5. 📋 **增加动作执行状态监控**
   - 影响：便于调试和问题排查
   - 风险：低
   - 工作量：2-3小时

6. 📋 **增加错峰参数可配置**
   - 影响：可以根据不同硬件调整错峰时间
   - 风险：低
   - 工作量：1-2小时

## 五、验收标准

### 5.1 功能验收

- ✅ 耳朵动作流畅，无"咔咔"声
- ✅ 序列动作按预期执行，无动作重叠
- ✅ 动作切换平滑，无明显延迟
- ✅ speaking 期间耳朵动作不影响音频播放

### 5.2 性能验收

- ✅ Worker Task 无长时间阻塞（>10ms）
- ✅ 串口连接稳定，无中断
- ✅ 动作响应延迟 <100ms
- ✅ 序列执行时间误差 <10%

### 5.3 稳定性验收

- ✅ 连续执行10轮 `excited` 序列无异常
- ✅ 快速连续触摸20次无崩溃
- ✅ 长时间运行（24小时）无内存泄漏
- ✅ 弱网环境下动作执行稳定

## 六、风险评估

### 6.1 修改 StartBothWithStagger

**风险**：低
- 错峰逻辑可能对某些硬件是必要的
- **缓解措施**：可以先在测试环境验证，如果无问题再移除；或者改为可配置

### 6.2 修改 MoveBoth 动作切换逻辑

**风险**：低
- 动作切换逻辑变化可能影响现有序列
- **缓解措施**：充分测试各种情绪序列，确保行为一致

### 6.3 移除 Worker Task 长时间阻塞

**风险**：中
- speaking 首包窗口期的延迟是为了避免电源波动
- **缓解措施**：改为非阻塞延迟（定时器），保持延迟效果但避免阻塞

## 七、测试计划

### 7.1 单元测试

1. 测试 `MoveBoth()` 的动作切换逻辑
2. 测试 `StartBothWithStagger()` 的非阻塞实现
3. 测试序列定时器与停止定时器的同步

### 7.2 集成测试

1. 连续执行各种情绪序列（happy, excited, playful等）
2. 快速连续触摸测试（20次/秒）
3. speaking 期间触发耳朵动作

### 7.3 压力测试

1. 24小时长时间运行
2. 弱网环境测试（30%丢包率）
3. 高负载测试（音频播放 + 耳朵动作 + 网络通信）

## 八、后续优化建议

1. **引入动作队列去重机制**：如果短时间内收到相同动作，只执行最后一次
2. **增加动作执行超时保护**：如果动作执行超过预期时间，自动停止并重置
3. **优化GPIO操作**：减少GPIO操作频率，使用硬件定时器控制电机
4. **增加动作执行监控**：记录每个动作的实际执行时间和状态，便于优化

---

**报告日期**：2025-01-XX
**问题发现**：耳朵控制不丝滑，出现"咔咔"声；串口连接中断
**根本原因**：Worker Task阻塞违反零阻塞原则；动作快速切换时的状态竞态
**修复优先级**：高
**预计修复时间**：2-3天
